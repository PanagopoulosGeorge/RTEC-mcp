"""Adapters — concrete implementations of the port Protocols.

Adapters depend on ports (for the interface) and on ``rtec_llm.types`` (for
data shapes). They never depend on core business logic
(generation / repair / query / evaluation). See ``docs/ARCHITECTURE.md``.
"""
